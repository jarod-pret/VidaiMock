{
  "id": "{{ uuid() }}",
  "object": "list",
  "data": [
    {% for i in range(end=5) %}
    {
      "index": {{ i }},
      "score": {{ random_float(min=0.0, max=1.0) }},
      "category_id": {{ random_int(min=100, max=999) }}
    }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
