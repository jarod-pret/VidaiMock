{#
   OpenAI embeddings response template.

   Shape matches real /v1/embeddings responses:
     - object: "list" (not "chat.completion")
     - data[]: array of {object: "embedding", embedding: [...], index: N}
     - usage: prompt_tokens + total_tokens only (NO completion_tokens)
     - model name in response

   Uses a short dummy embedding vector (8 floats). Tests assert on
   shape (field names, types, usage structure), not on embedding
   values or dimensionality.
#}
{
  "object": "list",
  "data": [
    {
      "object": "embedding",
      "embedding": [0.0023, -0.0091, 0.0156, -0.0032, 0.0084, -0.0045, 0.0112, -0.0067],
      "index": 0
    }
  ],
  "model": "{{ json.model | default(value='text-embedding-3-small') }}",
  "usage": {
    "prompt_tokens": 2,
    "total_tokens": 2
  }
}
